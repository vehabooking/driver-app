<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (http) is allowed ONLY for local development hosts. Production
  traffic (vehabooking.com over https) is unaffected and stays encrypted.
  - vehabooking.test : Herd local domain
  - 10.0.2.2         : Android emulator alias for the host machine's localhost
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">vehabooking.test</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <!-- LAN address of the dev machine, for testing a debug build on a
             real phone against `php artisan serve --host=0.0.0.0`. -->
        <domain includeSubdomains="true">192.168.1.4</domain>
    </domain-config>
</network-security-config>
